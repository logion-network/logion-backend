/**
 * A tokenization request is submitted by a requester to a legal officer. A newly submitted
 * request is initially pending. The legal officer later decides to either accept or reject it.
 */
@Aggregate
package logion.backend.model.tokenizationrequest;

import logion.backend.annotation.Aggregate;
